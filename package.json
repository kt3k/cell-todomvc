{
  "private": true,
  "name": "cell-todomvc",
  "description": "TodoMVC of Cell",
  "scripts": {
    "test": "start-server-and-test start http://localhost:8000 'cypress run'",
    "start": "deno task start"
  },
  "dependencies": {
    "todomvc-app-css": "^2.3.0",
    "todomvc-common": "^1.0.5"
  },
  "devDependencies": {
    "cypress": "^6.0.0",
    "start-server-and-test": "^2.0.4"
  }
}
